================================================================================
Pjay:

Si pas de fichier conf fourni en argument en prendre un par defaut

si page erreur alors connection close
autres pages: preciser keep-alive

Limit client body size in conffile et stocker dans le serv pars

creer une table qui connait tout les types de fichers pour le rajouter apres avoir trouver le type : html = text/html jpeg = image/jpeg

preciser les methodes autorisees dans les differentes routes
Code la methode delete
ameliorer le fonction qui creer un errpage en fonction du code d'erreur
Elle doit pouvoir etre appele comme tu veux
Avec un post regarde comment fonctionne le content de la request
===============================================================================
rertzer:

fermeture controlee d'un socket:
int shutdown(int sock, int mode);
mode :
0 socket ne recoit plus
1 socket n'emet plus
2 close
cote client:d'abord on shutdown 1 puis on close apres reponse du serveur

Envoyer les bons messages d'erreur


gerer le champ connection et ses fields

Vider les buffers

nom des champs case insensitive

implementer setContentByChunked

si aucun socket ouvert, fermer le programme

directory indexing: autoindex on/off

finir les handle event
allow_methods
client_max_body_size
client_body_temp_path

